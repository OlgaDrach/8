#2)виведіть на екран транспоновану матрицю 3*3 (початкова матриця задана користувачем).

import numpy
#знову створюєм масив з нулів (тип цілих чисел)
o = numpy.zeros((3,3), dtype = int)
for f in range(3):
    for j in range(3):
        # міняємо містями e i d , і тоді матриця залишается такою ж, а ітерація йде вертикально
        o[j,f]=int(input('A['+str(f)+','+str(j)+']='))
print(o)
